import type {
  EmptyObject,
  EventDeclarations,
  PropDeclarations,
} from "@aria-ui/core"

/**
 * @group TooltipTrigger
 * @internal
 */
export interface TooltipTriggerProps extends EmptyObject {}

/**
 * @group TooltipTrigger
 * @internal
 */
export const tooltipTriggerProps: PropDeclarations<TooltipTriggerProps> = {}

/**
 * @internal
 */
export interface TooltipTriggerEvents extends EmptyObject {}

/**
 * @internal
 */
export const tooltipTriggerEvents: EventDeclarations<TooltipTriggerEvents> = {}
