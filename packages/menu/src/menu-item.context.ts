import { createContext } from "@aria-ui/core"

export const selectedValueContext = createContext("MenuItem/selectedValue", "")

export const focusedValueContext = createContext("MenuItem/focusedValue", "")
