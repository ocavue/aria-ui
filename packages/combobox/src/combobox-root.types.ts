import type {
  EmptyObject,
  EventDeclarations,
  PropDeclarations,
} from "@aria-ui/core"

/**
 * @group ComboboxRoot
 * @internal
 */
export interface ComboboxRootProps extends EmptyObject {}

/**
 * @group ComboboxRoot
 * @internal
 */
export const comboboxRootProps: PropDeclarations<ComboboxRootProps> = {}

/**
 * @group ComboboxRoot
 * @internal
 */
export interface ComboboxRootEvents extends EmptyObject {}

/**
 * @group ComboboxRoot
 * @internal
 */
export const comboboxRootEvents: EventDeclarations<ComboboxRootEvents> = {}
