import type {
  EmptyObject,
  EventDeclarations,
  PropDeclarations,
} from "@aria-ui/core"

/**
 * @group SelectTrigger
 * @internal
 */
export interface SelectTriggerProps extends EmptyObject {}

/**
 * @group SelectTrigger
 * @internal
 */
export const selectTriggerProps: PropDeclarations<SelectTriggerProps> = {}

/**
 * @group SelectTrigger
 * @internal
 */
export interface SelectTriggerEvents extends EmptyObject {}

/**
 * @group SelectTrigger
 * @internal
 */
export const selectTriggerEvents: EventDeclarations<SelectTriggerEvents> = {}
